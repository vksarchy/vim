# The secret sauce here 


core idea - chain commands , pipe symbol 

ls | grep '*.py' | sort 

echo "hello" > test.txt
echo "wordld" >> test.txt

double && - only if the 1st command succeeds 
double || - run the second command, only if the 1st fails, (fallback error)
